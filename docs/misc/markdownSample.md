
# 1. 見出しH1（クリックで配下を折りたたみ可能）

見出しの左側をクリックすると、次の同レベル以上の見出しまでのコンテンツが表示・非表示で切り替わります。

## 1.1 見出しH2

テキストの装飾テスト：
*斜体（イタリック）* と **太字（ボールド）** と ***太字斜体*** 
~~打ち消し線~~ と `インラインコード`
==Obsidian互換のハイライト文字== も正しく表示されます。

---

## 2. タスクリスト（インタラクティブ＆ハイライト動作確認）

- [ ] 親タスク 1（クリックすると即座にファイル保存されます）
  - [ ] 子タスク 1-1（ネスト・字下げ表示の確認）
  - [x] 子タスク 1-2（完了済みタスク：丸型アクセントカラー表示）
- [x] 親タスク 2（チェック済み行全体のハイライト色適用確認）
  - [ ] 子タスク 2-1
  - [ ] 子タスク 2-2

---

## 3. コードブロック（コピペボタンの動作確認）

マウスホバーで右上にコピー用アイコンが表示されます。

```typescript
// TSコードのサンプル
function calculateTotal(items: number[]): number {
  return items.reduce((sum, item) => sum + item, 0);
}

console.log(calculateTotal([10, 20, 30]));
```

```python
# Pythonコードのサンプル
def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("Markdown Editor"))
```

---

## 4. 箇条書き・番号付きリスト

通常の箇条書き：
- りんご
- みかん
  - 温州みかん
  - 伊予柑
- ぶどう

順序付きリスト：
1. 最初のステップ
2. 次のステップ
   1. サブステップ A
   2. サブステップ B
3. 最後のステップ

---

## 5. 引用（Blockquote）

> これは引用ブロックです。
> 複数行の引用テキストも綺麗に表示されます。
>> ネストされた引用文

---

## 6. テーブル（表）

| 機能名 | 状態 | 備考 |
| :--- | :---: | ---: |
| タスクチェック | 完成 | インタラクティブ保存 |
| コードコピー | 完成 | ワンクリックコピー |
| 見出し折りたたみ | 完成 | Obsidian互換デザイン |

---

## 7. リンク & Obsidian画像記法

- 外部リンク: [Tauri 公式サイト](https://tauri.app/)
- Obsidian画像記法: `![[sample-image.png]]` （※画像フォルダ設定時に読み込まれます）

---

### 見出しH3（下位レベル見出しテスト）
ここも見出しH1やH2の折りたたみ範囲に正しく連動します。
