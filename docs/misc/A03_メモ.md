
## 色設定
style="background-color: var(--bg-color); color: var(--text-color);"
var(--menu-bg)

hover:bg-[var(--変数名)] : Tailwindで用意されていない色でも、テーマ変数などを直接ホバー時の指定として使えます。
hover:brightness-110 : ボタンなどにホバーした際、「今設定されている色（アクセントカラー等）」をそのまま少しだけ明るくする（110%にする）非常に便利なTailwindのクラスです。テーマに関係なく使えます。
opacity-50 など : 文字色を gray-400 などにする代わりに、現在のテキストカラーのまま不透明度を50%にすることで、ダーク・ライト問わず綺麗に馴染む薄い文字を作れます。

 <div 
                    class="py-1.5 px-2 border-b cursor-pointer transition hover:bg-[var(--active-highlight-bg)]" 
                    style="border-color: color-mix(in srgb, var(--text-color) 10%, transparent);"
                >

color-mix(in srgb, var(--text-color) 20%, transparent)
＝「var(--text-color) を 20%、transparent (透明) を残り（80%）で混ぜる」という意味になります。


### 新しいチャットに貼っていただきたいファイル一覧

1. **`src/components/Editor/markdownsetup.ts`**
   - **理由**: マークダウンの記法を解析してプレースホルダーを作る部分です。検索の `(キーワード, 10)` のような件数指定のパースや、新しいタスク用記法（例: `obcowa-tasks`）のルールをここに追加します。
2. **`src/features/Dashboard/DashboardManager.ts`**
   - **理由**: 見つけたプレースホルダーにウィジェットをマウントする司令塔です。ここに新しいタスクウィジェットを呼び出す処理を追加します。
3. **`src/features/Dashboard/widgets/SearchWidget.svelte`**
   - **理由**: これからリミット件数を受け取って表示を制限できるように直接修正を加えるファイルです。
4. **`src/features/Task/TaskList.svelte` (または `TaskItem.svelte`)**
   - **理由**: これから作るタスクウィジェットのUIやチェックボックスの処理を、既存のアプリの表示と統一するために参考にします。
5. **`src/lib/task/taskService.ts`**
   - **理由**: Tauri（バックエンド）からタスクを取得・更新するための既存のAPI（関数）がどうなっているかを確認し、タスクウィジェットから呼び出すために必要です。

---

### 次のチャットへの引き継ぎメモ（おまけ）
新しいチャットの最初に、ファイルと一緒に以下のメモを添えていただくと、AIが文脈をすぐに理解できます！

> **【目的】**
> Markdownダッシュボードの動的ウィジェット機能を拡張したい。
> 
> **【やりたいこと】**
> 1. 検索ウィジェット（`SearchWidget.svelte`）に表示件数のリミット機能をつけたい。記法は ` ```obcowa-search(メモ, 5) ``` ` のような形を想定。
> 2. ワークスペース内の未完了タスク一覧を表示する新しいタスクウィジェット（`TasksWidget.svelte`）を作成したい。記法は ` ```obcowa-tasks ``` ` を想定。既存のTaskListのUIやtaskServiceのロジックを再利用したい。

それでは、新しいチャットでお待ちしております！



Obsidian Vaultのドキュメントを自作ビューアで快適に閲覧・編集するために、移植の優先度が高い主要な記法とそれぞれの仕様です。

**優先度の高いObsidian固有記法一覧**

| 記法 | 入力例 | 主な用途 | 実装優先度 |
| --- | --- | --- | --- |
| **WikiLink（内部リンク）** | `[[ノート名]]`<br>

<br>`[[ノート名|表示名]]` | ノート間の相互参照 | ★★★（最優先） |
| **埋め込み（Transclusion）** | `![[画像.png]]`<br>

<br>`![[ノート名]]` | 画像・別ノートの直接埋め込み | ★★★（最優先） |
| **Callout（吹き出し）** | `> [!NOTE]`<br>

<br>`> 内容` | 注記や警告ブロックの装飾 | ★★★（高） |
| **画像サイズ指定** | `![[image.png|300]]`<br>

<br>`![alt|300](path)` | 幅・高さの明示指定 | ★★☆（中〜高） |
| **ハイライト** | `==マーカー文字==` | テキストの蛍光ペン強調 | ★★☆（中） |
| **コメント** | `%% プレビュー非表示 %%` | ビューア上で非表示にするメモ | ★★☆（中） |
| **タグ / 階層タグ** | `#tag`<br>

<br>`#parent/child` | 分類・メタデータ付与 | ★★☆（中） |
| **拡張タスク** | `- [/]`（進行中）<br>

<br>`- [-]`（打ち消し） | チェックボックス状態の拡張 | ★☆☆（低〜中） |
| **見出し・ブロック参照** | `[[ノート#見出し]]`<br>

<br>`[[ノート#^blockid]]` | 特定箇所へのピンポイント参照 | ★☆☆（低） |

---

**主要記法の実装メリットと技術的課題**
* **Callout `> [!NOTE]**`
* **メリット：** 仕様書や開発ログの見栄えが一気に整い、視認性が大きく向上します。
* **課題：** `NOTE`, `WARNING`, `TIP` などのタイプに応じたCSSクラスの割り当てや、折りたたみ機能（`> [!NOTE]-`）のレンダリング処理が必要です。

* **画像サイズ指定 `![[image.png|300]]**`
* **メリット：** 巨大な画像が画面を埋め尽くすのを防げます。
* **課題：** パイプ記号の後の数値（`300` や `300x200`）を解析し、`<img>` タグの `width` / `height` スタイルへ変換する処理を挟む必要があります。

---

**折りたたみ（畳み込み）機能の記法**

タイトル行の末尾に `+` または `-` を付与することで、ユーザーがクリックして開閉できる折りたたみブロックになります。ビューアを自作する際は `<details>` タグ等へ変換する処理で再現可能です。

* **デフォルトで開いておく:** `> [!NOTE]+ タイトル`
* **デフォルトで閉じる:** `> [!NOTE]- タイトル`


---

**パーサー移植のアプローチ（グラデーション）**

1. **Rust / ASTレベルでの構文解析（堅牢性重視）**
* **概要：** MarkdownをAST（構文解析木）に変換する際、WikiLinkやCallout用のカスタムノードとしてパースする。
* **評価：** 処理が超高速で構造的にも安全ですが、パースロジック自体の自作・拡張が必要です。


2. **HTML変換前後の正規表現置換（開発速度重視）**
* **概要：** MarkdownからHTMLへ変換する前後のタイミングで、`[[...]]` や `==...==` を正規表現で `<a>` や `<mark>` に書き換える。
* **評価：** 数行〜数十行の処理で爆速実装できますが、コードブロック（``` 内）に書かれた文字列まで誤って置換しないよう除外処理が必要です。

---

このsvelteファイルのUIの、「ファイル名のみ検索」と「ライブラリを含めない」なのですが
lucideのアイコンでONOFFをトグルする形にしてほしいです