---
tags:
  - P/App/Obcowa
---

## リリースで確認すること
アイコンとアプリ名: tauri.conf.json や src-tauri/tauri.conf.json で、アプリ名（productName）やバージョン、アイコンがデフォルトの設定（Tauriのロゴなど）のままになっていないか確認しましょう。

スマートフォルダの「条件に一致するファイルをリスト化」機能で、誤って削除・移動などの操作をしてしまった時に取り消せるか
Vaultのパスが変わった/フォルダが移動した時に、参照切れでクラッシュしないか

TauriのCSP設定（tauri.conf.jsonのcsp）がちゃんと絞られているか
invokeで呼び出せるRustコマンドの権限スコープ（任意パスの読み書きを許してしまっていないか、Vault外への書き込みができてしまわないか）
Markdown内のHTML/リンクを開く処理で、file://や外部URLのハンドリングが安全か


<script>あれえ？</script>

==tesuto==

スマートフォルダの管理
アイコンを分けて、整理用フォルダも作れるようにする
その中にスマートフォルダ内のファイルを入れるのも可能
パスが存在し続ければ、整理用フォルダにずっといる
変更されたら、スマートフォルダの中に戻る
スマートフォルダをそのまま使ってもいいし、ソースとして使って整理用フォルダに分けてもいい

リストを管理から編集できるようにするか

## v1.0.0までに入れる機能
サイドバー
スマートフォルダまわり

タグ削除を有効に

タブ開いてない状態で、ちょっと説明入れる
リンク更新したときもリストを更新する

マークダウンの見出しを隠す
アウトライン

ライブラリも更新で更新する

デフォルトで、H1だけ下線するように

自動保存したないようと自分の編集を競合だと判定してない？

表が表示くずれる

### テーマ系

ワークスペース内であればリンク可に

テーマはワークスペースごとに設定できるように
### サイドメニュー


全体から
タグ検索やりたい
タグがあらかじめビューされてる

フォルダの一番上層を表示しない（中身を直接リストに）

ワークスペース抽出のフォルダは、エクスプローラーで見れないようにする　実体がないから

ファイル単体で追加したものは、フォルダにまとめられるようにする

スマートフォルダ
プロパティで絞れるように

一番上の階層単位で、フォルダの位置を変えられる（カテゴリ作れる）

リンクの文字色


左上のワークスペース名消す

#### バグ
スマートフォルダ
結果が０件だったときの挙動

スマートフォルダ　再編集で、フォルダ構成変えられない
メニューを右クリックメニューのタグを挿入がサブメニューが出ない

左のメニューがなんか左側のラインががたがたしてる
ソート効いてるんかこれ？

ワークスペース全体　ライブラリも含める

### エディタ
検索　結果を作成日・更新日でソート

今日編集したファイルリスト　編集画面で右クリックで挿入
リスト形式にする、Obリンク、改行せずにカンマで区切る
新規作成したファイルリスト
デイリーノート作成
編集後、プロパティ部分の文法が崩れていたら警告を出す
タブを右クリックで、ピン留めへ　エクスプローラーで開く

アウトライン機能


最近閉じたタブ
Ctrl+Shift+T
#### バグ
なんか保存頻繁すぎん？

### ベース？
ライブラリを登録したら、リストを更新
ライブラリ　最上位のフォルダを表示するかどうか
起動時白画面出ないように

htmlはちょっと悪意あるの大丈夫？

再フォーカス処理する
タイトルバーどうしよう？　色設定できるようにする？

### 複数
メニュー側のフォント


### バグフィックス

タグを削除したら、残り一個だった場合、タグのプロパティごと削除してる　これは好き好きだけど…うーん。


## リファクタリングねた
HTMLに直書きしてる色設定、CSSに移す

## その他

全体的に、もっと行間をつめる（左メニュー）


スぺニット？　はワンボタンでコピー可にする

## 2. 「このファイルを含むワークスペース」
右クリック

このファイルが含まれるワークスペース

・Tauri
・Rust
・記事執筆

---

## 3. ワークスペースごとのメモ

例えば左上に


今日やること

□ スマートフォルダ
□ タグ追加
□ バグ修正


---

## 5. タグ統計


todo 12
clip 31
ongoing 5


## 7. 「一時ワークスペース」

Ctrl+N
Temporaryを作る。

そこへ今必要なファイルをドラッグ。

終了したら消える。

毎回Workspace作るほどじゃない時用。

## 9. Ctrl+数字でタブ切り替え


---

## 個人的に一番欲しいのはこれ


**「コンテキストに追加」**

です。

例えばExplorerから
魔法.md
を右クリック
現在のWorkspaceへ追加

あるいは、おぶこわ内で
Ctrl+Shift+A
今開いているWorkspaceに登録

